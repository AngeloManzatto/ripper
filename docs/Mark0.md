[x] - Goal detection + episode outcome — reward/done, our first multi-value return
Episode loop — running a real sequence of actions, not one hardcoded call
Terminal rendering — readable grid output, first custom trait (Display)
Config constants — clean up magic numbers before the project grows
Modules — split files, mirrors .h/.cpp separation you already know
Scale to 10x10 — the actual Mark 0 finish line